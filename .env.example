# Copy to .env.local. Vercel: lowercase names are fine.

journal_sync_secret=

# From Storage when the Blob store is linked (BLOB_READ_WRITE_TOKEN on Vercel)
blob_read_write_token=

# Optional: legacy blob path if you uploaded an older backup filename
# journal_legacy_blob_path=trade-log-backup-2026-05-19.json
